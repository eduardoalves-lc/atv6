import React from 'react';
import {SafeAreaView} from 'react-native';
import Firebase from './components';

const App = () => {
  return (
    <SafeAreaView style={{flex: 1}}><Firebase /></SafeAreaView>
  );
};

export default App;
